const NotFound = () => {
    return(
        <div style={{
            display: "flex",
            alignItems: "center", 
            justifyContent: "center",
            fontSize: 64,
            position: "absolute",
            width: '100%',
            height: '100%',
            color: 'red',
        }}>
            404
        </div>
    );
}

export default NotFound;